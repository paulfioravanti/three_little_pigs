require "spec_helper"

describe ThreeLittlePigs do
  it "has a version number" do
    expect(ThreeLittlePigs::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
